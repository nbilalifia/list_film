//ValueNotifier
//ValueListenableBuilder

import 'package:flutter/material.dart';

ValueNotifier<int> selectedPageNotifier = ValueNotifier(0); // Initialize the selected index to 0
ValueNotifier<bool> isDarkModeNotifier = ValueNotifier(true); // Initialize dark mode to false